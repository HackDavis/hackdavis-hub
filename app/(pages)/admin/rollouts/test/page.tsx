import TimeProtectedDisplay from '@pages/_components/TimeProtectedDisplay/TimeProtectedDisplay';

export default function Test() {
  return <TimeProtectedDisplay featureId="123">hi</TimeProtectedDisplay>;
}
