name: Development Check

on:
  pull_request:
    branches:
      - "*"

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    environment:
      name: development
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install node
        uses: actions/setup-node@v4
        with:
          node-version: "21.7.3"

      - name: Install dependencies
        run: npm install

      - name: Build Next.js app
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
        run: npm run build

      - name: Debug compiled files
        run: |
          cat .next/server/chunks/4154.js | head -n 200
          echo "=== Checking for authorize function around line 9966 ==="
          cat .next/server/chunks/4154.js | tail -n +9950 | head -n 50

      - name: Run tests
        run: BASE_URL=${{ vars.ENV_URL }} ADMIN_EMAIL=${{ secrets.ADMIN_EMAIL }} ADMIN_PASSWORD=${{ secrets.ADMIN_PASSWORD }} ADMIN_PASSWORD_HASHED=${{ secrets.ADMIN_PASSWORD_HASHED }} AUTH_SECRET=${{ secrets.AUTH_SECRET }} npm run test:debug

      - name: Success
        run: echo "🚧 Build successful 🚧"
